export const mapCoverageLevel = (level: "low" | "standard" | "high") => {
  return level;
};

export const mapContractPriority = (priority: 1 | 2 | 3) => {
  return priority;
};

export const mapEquityPriority = (priority: "low" | "standard" | "high") => {
  return priority;
};
